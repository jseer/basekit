import deferred, { IDeferred } from './deferred';
import timeout from './timeout';
import promisifyCall from './promisifyCall';

export {
    IDeferred
}

export {
    deferred,
    timeout,
    promisifyCall,
}
